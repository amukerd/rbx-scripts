```
loadstring(game:HttpGet("https://amukerd.github.io/rbx-scripts/cdt/placetp.lua"))()
```

change workspace deletion to tp away and only not delete the baseplate no need for random nested logic to not delete the cyl things
might not be good if a dev joins tho, need to make mod check

serverscriptservice.services.tradinghubservice:436 offer already exists line 163
serverscriptservice.services.tradinghubservice:439 you have already offered all of this customization item
function listcustomizationitems
need to fix error

want to open localplayer claimed booth ui on join (or some kind of button) so its possible to see listed items

~~need to remove print statements for checking Rap, fullproof system so logging isnt necessarily needed anymore~~
