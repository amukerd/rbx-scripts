loadstring(game:HttpGet("https://raw.githubusercontent.com/amukerd/rbx-scripts/refs/heads/main/cdt/placetp.lua"))()

```python
print("This code block has a copy button")


### Alternative: Plain Text Copyable Blocks
If you need plain text (not code) to be easily selectable or have a copy button, you can use a **markdown header followed by two tabs**.  This indents the text, making it selectable as a block. 

```markdown
## 
	Your copyable text here   
